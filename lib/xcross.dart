/// xcross: build, run, and hot-reload Flutter iOS apps from Linux without
/// Xcode, driving the original xtool CLI for device discovery, signing, and
/// install.
library;

export 'src/cli/runner.dart' show XcrossCli;
